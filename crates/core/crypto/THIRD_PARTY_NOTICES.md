# Third-party notices

The SHAKE-256, Ed25519, and witness-chain implementations are derived from
`rvf-crypto` in the RuVector project (`https://github.com/ruvnet/ruvector`),
package version 0.2.0. The donor package declares `MIT OR Apache-2.0`; this
derived crate uses the MIT option. RVF-specific types and runtime behavior were
not copied.
